<div class="wfrp4e chat-card">
    <div class="opposed-card">
        <div>
            {{{result}}}
        </div>
        <div>
            {{{damage.description}}}
        </div>
        <div>
            {{{hitloc.description}}}
        </div>
        {{#each other as |string index|}}
        <div>
            {{{string}}}
        </div>
        {{/each}}
        {{#if damage.value}}
            <a class="apply-damage">Zadaj obrażenia</a>
        {{/if}}
    </div>
</div>